import { CrimeIncident } from '../../models/crime.model';

// Patrol log connector stub — implement streaming or DB fetch
export async function pollPatrolLogs(): Promise<CrimeIncident[]> {
  // TODO: integrate with patrol logs source (PATROL_STREAM_URL)
  return [];
}


